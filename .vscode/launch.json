{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File",
      "type": "debugpy", // Use debugpy type
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal"
    },
    {
      "name": "Python: Launch main.py",
      "type": "debugpy", // Use debugpy type
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "args": [
        // Provide the positional IDF file argument
        "${workspaceFolder}/tests/_20.4in.idf",
        // Provide the required --idd argument
        "--idd",
        "C:/EnergyPlusV9-4-0/Energy+.idd" // Assuming this path based on settings.json. Adjust if needed.
        // Optionally add --output argument if needed:
        // "--output", "${workspaceFolder}/debug_output"
      ],
      "console": "integratedTerminal"
    }
  ]
  // Remove the "inputs" section if you added it previously
}
